# ============================================
# Railway Environment Variables Template
# ============================================
# Copy these to Railway dashboard for each service
# DO NOT commit actual secrets to GitHub!

# ============================================
# SERVER SERVICE VARIABLES
# ============================================
# Add these to your "movieproject-server" service in Railway

NODE_ENV=production
PORT=3001

# TMDB API Key (get from themoviedb.org)
TMDB_API_KEY=your_tmdb_api_key_here

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here

# PostgreSQL Connection (use Railway references)
POSTGRES_HOST=${{Postgres.POSTGRES_HOST}}
POSTGRES_USER=${{Postgres.POSTGRES_USER}}
POSTGRES_PASSWORD=${{Postgres.POSTGRES_PASSWORD}}
POSTGRES_DB=${{Postgres.POSTGRES_DB}}
POSTGRES_PORT=${{Postgres.POSTGRES_PORT}}

# ============================================
# CLIENT SERVICE VARIABLES
# ============================================
# Add these to your "movieproject-client" service in Railway

# Backend API URL (use your Railway backend domain)
VITE_API_BASE_URL=https://your-backend-name.up.railway.app

# ============================================
# NOTES:
# ============================================
# 1. Replace "your_tmdb_api_key_here" with actual TMDB API key
# 2. Replace "your_jwt_secret_here" with generated secret
# 3. Replace "your-backend-name" with actual Railway backend domain
# 4. The ${{Postgres.VARIABLE}} syntax auto-references Railway PostgreSQL service
# 5. Never commit this file with real values to GitHub!